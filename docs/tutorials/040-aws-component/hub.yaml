kind: stack
version: 1
meta:
  name: My aws deployment

requires:
  - aws
  - terraform

components:
  - name: my-awshubctl-component
    source:
      dir: components/s3-bucket

extensions:
   configure:
    - aws
    - env

